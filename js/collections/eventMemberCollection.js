var app = app || {};

app.EventMemberCollection = Backbone.Collection.extend({

    model: app.EventMemberModel

    //initialize: function (options) {
    //    this.search_model = options.search_model;
    //}
});
